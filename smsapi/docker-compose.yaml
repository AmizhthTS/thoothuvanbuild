version: '3.3'

services:
   masterapilb:
    image: amizhth/thoothuvan:1.0
    restart: always
    ports:
      - '6003:8080'
    environment:
      - TZ=Asia/Kolkata
networks:
  default:
    name: thoothuvan
    external: true
